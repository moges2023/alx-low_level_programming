#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with the first number
 *
 * Return: The first of muliplcation between x param and y param
 */
int mul(int x,int y)
{
    return (x*y);
}
