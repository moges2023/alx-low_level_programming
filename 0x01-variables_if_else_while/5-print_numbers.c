#include <stdio.h>

/**
 * main - prints all strings from 0 to 9
 * Return: always 0 (success)
*/
int main(void)
{
	int num;

	for (num = 0; num < 10; nub++)
		printf("%d", num);

	pintf("\n");

	return (0);
}
