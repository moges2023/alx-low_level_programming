#include <stdlib.h>
/**
 * main - headers goes there
 * discribtion:"prints numbers of base 10 using putchar"
 * Return: always 0
*/
int main(void)
{
	int n;

	for (n = 0; n <= 9; i++)
	{
		putchar(n + 48);
	}
	putchar('\n');
	return (0);
}
